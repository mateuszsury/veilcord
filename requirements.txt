# DiscordOpus Python Dependencies
# Phase 1: Cryptographic Foundation & Packaging

# Cryptography for key generation and encryption
cryptography==46.0.4

# Windows API access for DPAPI key protection
pywin32>=306

# SQLCipher for encrypted local database
# Note: sqlcipher3 (not -binary) has Python 3.13 wheels
sqlcipher3>=0.6.2

# PyWebView for desktop window with React frontend
pywebview>=6.1

# Argon2 for password hashing (master password)
argon2-cffi>=23.1.0

# PyInstaller for single .exe packaging
pyinstaller>=6.18.0

# Phase 2: Signaling Infrastructure & Presence

# WebSocket client for signaling server connection
websockets>=15.0

# Phase 3: P2P Text Messaging

# Python WebRTC implementation for P2P data channels
aiortc>=1.14.0

# Signal Protocol for E2E encryption
DoubleRatchet>=1.3.0
X3DH>=1.1.0

# Phase 4: File Transfer

# Image processing for thumbnails
Pillow>=10.0.0

# Video thumbnail extraction
ffmpeg-python>=0.2.0

# Phase 5: Voice Calls

# Audio device enumeration and recording/playback
# Auto-installs PortAudio on Windows
sounddevice>=0.5.0

# Required by sounddevice for audio array processing
numpy>=2.0.0

# Opus audio encoding/decoding
# Bundles libopus DLLs for Windows, provides libopusenc for Ogg writing
PyOgg>=0.6.14

# Phase 6: Video & Screen Sharing

# Camera capture via cv2.VideoCapture
opencv-python>=4.0

# Cross-platform screen capture (30-60 FPS)
mss>=9.0

# Camera enumeration with names (OpenCV lacks this)
cv2-enumerate-cameras>=1.3.3

# Phase 8: Notifications & Polish

# Windows toast notifications with interactive buttons
windows-toasts>=1.3.1

# Secure auto-updater with TUF cryptographic verification
tufup>=0.10.0

# Phase 9: Audio & Video Effects

# AI-based noise suppression (PESQ 3.5-4.0+)
deepfilternet>=0.5.6

# Lightweight noise cancellation for CPU fallback (10-20ms latency)
pyrnnoise>=0.4.3

# Spotify's production audio effects library
pedalboard>=0.9.21

# Google's face landmarks and segmentation
mediapipe>=0.10.32

# Optional: PyTorch for DeepFilterNet (CPU-only by default)
# torch  # User can install torch[cuda] for GPU acceleration

# Optional: GPU/system monitoring
psutil>=5.9.0
# GPUtil>=1.4.0  # NVIDIA only, optional
